// Ejemplo de configuración de Firebase. Copia este archivo a
// `config.local.js` y rellena los valores con los del proyecto.
// El PROJECT_ID es el identificador raíz dentro de Firestore, bajo el
// que cuelga todo (`projects/{PROJECT_ID}/...`).

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.firebasestorage.app",
  messagingSenderId: "0000000000",
  appId: "1:0000000000:web:xxxxxxxxxxxxxxxxxxxxxx",
  measurementId: "G-XXXXXXXXXX",
};

export const PROJECT_ID = "your-project-slug";
